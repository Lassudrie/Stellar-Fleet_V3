// Shared constants for contested army operations

// Probability threshold applied when unloading or deploying armies in contested orbits
export const CONTESTED_DROP_FAILURE_THRESHOLD = 0.35;

// Fraction of strength lost when a contested drop fails
export const CONTESTED_DROP_LOSS_FRACTION = 0.35;
